import { getServiceClient, hashBody, readBody, requireAdmin, requireIdempotencyKey, requireMethod, rpc, sendJson, withApi } from "./_lib.js"
export default withApi(async(req,res,traceId)=>{requireMethod(req,"POST");const user=await requireAdmin(req);const body=await readBody(req);const key=requireIdempotencyKey(req);const supabase=getServiceClient();const result=await rpc(supabase,"phase22_approve_task",{p_owner_user_id:user.id,p_task_id:body.taskId,p_idempotency_key:key,p_request_hash:hashBody(body),p_rationale:body.rationale||null,p_trace_id:traceId});if(result.state==="in_progress")return sendJson(res,202,{ok:false,status:"in_progress"},traceId);const{data,error}=await supabase.from("tasks").select("*").eq("id",body.taskId).eq("owner_user_id",user.id).single();if(error)throw error;sendJson(res,200,{ok:true,task:data},traceId)})
