export function CaseJourney({ stages, activeStageId, onSelect }) { return <nav aria-label="Case journey" className="case-journey">{stages.map((stage,index)=>{const disabled=!stage.available&&!stage.completed;return <button key={stage.id} type="button" aria-label={stage.label} disabled={disabled} aria-current={stage.id===activeStageId?"step":undefined} className={`case-journey__step case-journey__step--${stage.status}`} onClick={()=>onSelect(stage.id)}><span className="case-journey__number">{String(index+1).padStart(2,"0")}</span><span className="case-journey__copy"><strong>{stage.label}</strong><small>{stage.completed?"Complete":stage.available?"Available":stage.lockReason}</small></span></button>})}</nav> }
