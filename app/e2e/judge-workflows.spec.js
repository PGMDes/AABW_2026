import { expect,test } from "@playwright/test"
async function analyze(page,label){await page.goto("/");await page.getByRole("button",{name:"New Task"}).click();await page.getByLabel("Load demo scenario").selectOption({label});await page.getByRole("button",{name:"Analyze Task"}).click()}
test("fresh load opens the Action Queue",async({page})=>{await page.goto("/");await expect(page.getByRole("heading",{name:"Action Queue"})).toBeVisible();await expect(page.getByText("Recommendation Result")).toHaveCount(0)})
test("agent case requires explicit option selection before execution",async({page})=>{await analyze(page,"Agent path");await page.getByRole("button",{name:"Selection",exact:true}).click();await page.getByRole("button",{name:"Select option"}).first().click();await page.getByRole("button",{name:"Execution",exact:true}).click();await expect(page.getByTestId("agent-runner")).toBeVisible()})
test("hybrid case keeps execution locked before approval",async({page})=>{await analyze(page,"Hybrid path");await expect(page.getByRole("button",{name:"Selection",exact:true})).toBeDisabled();await page.getByRole("navigation",{name:"Case journey"}).getByRole("button",{name:"Governance",exact:true}).click();await expect(page.getByRole("heading",{name:"Human approval gate"})).toBeVisible()})
test("blocked case exposes no selection action",async({page})=>{await analyze(page,"Blocked");await expect(page.getByRole("button",{name:"Selection",exact:true})).toBeDisabled()})
test("reset clears local case state",async({page})=>{await analyze(page,"Agent path");await page.getByRole("button",{name:"Action Queue"}).click();await page.getByRole("button",{name:"Reset local demo state"}).click();await expect(page.getByRole("heading",{name:"Action Queue"})).toBeVisible()})
